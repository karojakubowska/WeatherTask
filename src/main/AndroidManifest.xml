<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.WeatherTask">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Recyclerview">
        <activity
            android:name="com.example.WeatherTask.StatsActivity"
            android:exported="false"
            android:parentActivityName="com.example.WeatherTask.MainActivity"/>
        <activity
            android:name="com.example.WeatherTask.DetailActivity"
            android:exported="true"
            android:parentActivityName="com.example.WeatherTask.MainActivity" />
        <activity
            android:name="com.example.WeatherTask.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>